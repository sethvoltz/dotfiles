nmap <leader>. :TlistToggle<CR>

" Open on the right side so as not to compete with nerdtree
let g:Tlist_Use_Right_Window = 1

" Only show the tags from the current file
let g:Tlist_Show_One_File = 1

" Give focus to the taglist on open - useful for quickly navigating between methods
let g:Tlist_GainFocus_On_ToggleOpen = 1
