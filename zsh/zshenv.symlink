# Huzzah special ZSH mappings http://stackoverflow.com/a/18077919/772207
# Give access to Homebrew packages
path=('./bin' '/opt/homebrew/bin' '/usr/local/bin' $path)

# Source in cargo environment
[ -s "$HOME/.cargo/env" ] && source "$HOME/.cargo/env"
